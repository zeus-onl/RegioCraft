[project]
name = "regiocraft"
description = "Put unlimited character LoRAs into one image, each locked to its own hand-drawn box — plus reference-image identity lock, cross-region attention dampening, per-region text conditioning, trigger-name auto-activation, and a sparse-token engine that only pays for the tokens that matter."
version = "1.0.0"
license = { file = "LICENSE" }

[tool.comfy]
PublisherId = "zeus-onl"
DisplayName = "RegioCraft"
Icon = ""
